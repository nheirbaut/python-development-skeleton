def is_even(number: int) -> bool:
    """Check if the given number is even.

    Args:
        number (int): The number to check for evenness.

    Returns:
        bool: True if the given number is even, False otherwise.

    """
    return number % 2 == 0
